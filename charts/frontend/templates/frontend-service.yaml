# templates/frontend-service.yaml

apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s" (include "app.name.fe" .) }}
spec:
  selector:
    app: {{ printf "%s" (include "app.name.fe" .) }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      nodePort: {{ .Values.service.nodePort }}
      protocol: TCP
      name: http
  type: LoadBalancer 